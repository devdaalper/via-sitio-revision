<div class="flow-visual" role="img" aria-label="Representación del proceso conectado de reclutamiento">
  <div class="flow-topline">
    <span>Vacante activa</span>
    <span class="status-dot">En proceso</span>
  </div>
  <div class="candidate-list" aria-hidden="true">
    <div class="candidate-row active"><i></i><span></span><strong>01</strong></div>
    <div class="candidate-row"><i></i><span></span><strong>02</strong></div>
    <div class="candidate-row"><i></i><span></span><strong>03</strong></div>
  </div>
  <div class="flow-path" aria-hidden="true">
    <span>Perfil</span><i></i><span>Entrevista</span><i></i><span>Decisión</span>
  </div>
  <div class="human-note">
    <span aria-hidden="true">✦</span>
    <p><strong>IA asistiva</strong> organiza el contexto para la revisión humana.</p>
  </div>
</div>

<style>
  .flow-visual {
    position: relative;
    width: min(100%, 520px);
    overflow: hidden;
    border: 1px solid rgba(39, 65, 79, 0.14);
    border-radius: 28px;
    background: #f9f9f9;
    box-shadow: 0 30px 70px rgba(22, 26, 35, 0.1);
    padding: clamp(22px, 4vw, 36px);
  }

  .flow-visual::after {
    position: absolute;
    right: -70px;
    bottom: -90px;
    width: 220px;
    height: 220px;
    border: 34px solid rgba(225, 254, 102, 0.36);
    border-radius: 999px;
    content: "";
  }

  .flow-topline {
    display: flex;
    align-items: center;
    justify-content: space-between;
    color: #27414f;
    font-size: 0.875rem;
    font-weight: 800;
  }

  .status-dot {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    color: #27414f;
    font-size: 0.875rem;
    font-weight: 700;
  }

  .status-dot::before {
    width: 8px;
    height: 8px;
    border-radius: 999px;
    background: #8dbd2f;
    content: "";
  }

  .candidate-list {
    display: grid;
    gap: 9px;
    margin-top: 26px;
  }

  .candidate-row {
    display: grid;
    min-height: 58px;
    grid-template-columns: 30px 1fr auto;
    align-items: center;
    gap: 14px;
    border: 1px solid rgba(39, 65, 79, 0.1);
    border-radius: 16px;
    padding: 10px 15px;
  }

  .candidate-row.active {
    border-color: rgba(141, 189, 47, 0.52);
    background: rgba(237, 235, 223, 0.56);
  }

  .candidate-row i {
    width: 30px;
    height: 30px;
    border-radius: 999px;
    background: #edebdf;
  }

  .candidate-row span {
    width: min(100%, 160px);
    height: 8px;
    border-radius: 999px;
    background: rgba(39, 65, 79, 0.16);
  }

  .candidate-row strong {
    color: #27414f;
    font-size: 0.875rem;
  }

  .flow-path {
    display: grid;
    grid-template-columns: auto 1fr auto 1fr auto;
    align-items: center;
    gap: 8px;
    margin-top: 28px;
    color: #27414f;
    font-size: 0.875rem;
    font-weight: 750;
  }

  .flow-path i {
    height: 1px;
    background: rgba(39, 65, 79, 0.22);
  }

  .human-note {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 12px;
    margin-top: 24px;
    border-radius: 17px;
    background: #161a23;
    color: #edebdf;
    padding: 16px 18px;
  }

  .human-note > span {
    color: #e1fe66;
  }

  .human-note p {
    margin: 0;
    font-size: 1rem;
    line-height: 1.55;
  }

  @media (max-width: 470px) {
    .flow-path {
      grid-template-columns: 1fr;
      gap: 3px;
    }

    .flow-path i {
      width: 1px;
      height: 14px;
      margin-left: 11px;
    }
  }
</style>
