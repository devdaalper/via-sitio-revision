{
  "name": "sitio-via",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check && npm run audit:colors && npm run audit:typography && npm run audit:logo && npm run audit:header && npm run audit:client-feedback && npm run audit:review-regressions",
    "audit:colors": "node scripts/audit-colors.mjs",
    "audit:typography": "node scripts/audit-typography.mjs",
    "audit:logo": "node scripts/audit-logo.mjs",
    "audit:header": "node scripts/audit-header.mjs",
    "audit:client-feedback": "node scripts/audit-client-feedback.mjs",
    "audit:review-regressions": "node scripts/audit-review-regressions.mjs"
  },
  "dependencies": {
    "@tailwindcss/vite": "4.3.3",
    "astro": "7.2.6",
    "tailwindcss": "4.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "typescript": "6.0.3"
  }
}
